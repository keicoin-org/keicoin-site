{
  "name": "keicoin-site",
  "version": "0.1.0",
  "private": true,
  "description": "keicoin.org — the site, on Cloudflare.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "bun run build:site && bun run build:docs",
    "build:site": "bun run build.ts",
    "build:docs": "vitepress build docs",
    "dev": "bun run build && wrangler dev",
    "docs:dev": "vitepress dev docs",
    "docs:preview": "vitepress preview docs",
    "deploy": "bun run build && wrangler deploy",
    "check": "bun run spec:check && bun run build && wrangler deploy --dry-run",
    "spec:check": "bun run src/site/spec-sheet-check.ts",
    "spec:write": "bun run src/site/spec-sheet-check.ts --write",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@keicoin/work": "^0.4.1",
    "kei-transaction": "0.8.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.0",
    "vitepress": "1.6.4",
    "wrangler": "^4.118.0"
  }
}
